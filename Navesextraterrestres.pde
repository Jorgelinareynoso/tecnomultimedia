class Naveextraterrestres{
  
  // -----------------------------------------------------
  // PROPIEDADES > variables
  // posición en x e y
  // velocidad en x
  // tamaño
 
  
  // -----------------------------------------------------
  // CONSTRUCTOR > el "setup de la clase"
  // inicializar las propiedades/variables
  // velocidad por random()
  
  // IMAGENES
  // carga de las imagenes

  // -----------------------------------------------------
  // METODOS > funciones
  
  // actualizar()
  // -- mueve las naves extraterrestres y llama a reciclar()

  // dibujar()
  // --dibuja la imagen en la posicion x/y de la clase
  
  // disparar()
  // --las naves disparan de forma aleatoria
  
   // mover()
  // --las naves se mueven en el eje x de forma aleatoria

  // reciclar()
  // --recicla todas las propiedades (valor inicial o random)
 
}
